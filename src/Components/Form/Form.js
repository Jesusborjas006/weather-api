import "./Form.css";

const Form = () => {
  return (
    <form className="form">
      <input className="city-input" type="text" placeholder="Search for a city..." />
    </form>
  );
};

export default Form;
